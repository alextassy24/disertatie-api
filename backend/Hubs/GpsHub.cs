using Microsoft.AspNetCore.SignalR;

namespace backend.Hubs
{
    public class GpsHub : Hub
    {
        // You can add methods here to handle client calls if needed
        // For now, it's empty because we are only broadcasting data from the server
    }
}
